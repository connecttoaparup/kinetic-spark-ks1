---
name: REPLACE-must-match-directory-name
description: >
  REPLACE. What the skill does AND when to use it, with trigger words a user
  would actually type. Max 1024 chars.
metadata:
  domain: REPLACE   # common | app | app-config | dag | dag-config
  owner: dip-platform
  version: "0.1"
---

# <Human Title>

<One line on the outcome.>

## Workflow
1. Gather inputs (ask if missing).
2. Plan to a temp location.
3. Validate with a bundled script; loop until pass.
4. Execute (write to the real location).
5. Confirm with the domain validator.

## Gotchas
- <Concrete correction to a mistake the agent makes without being told.>
