# pyspark-ingestion-app (mock - FIXED shared repo)
Shared PySpark ingestion application used by ALL projects.

NOTE: `barricade_migrate_service` and `tsql_to_bq_service` are SERVICES of this
app (etl/services/), NOT skills.
